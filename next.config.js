/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com", "example.com", "owen.cdn.vccloud.vn"], // thêm domain khác nếu cần
  },
};

module.exports = nextConfig;
